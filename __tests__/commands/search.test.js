// const search = require('../../commands/search');
// const Spinner = require('../../utils/spinner');

// describe('search.js command', () => {
//   it('throws an error when it has not been passed a callback function', async () => {
//     const spinnerInstance = jest.fn(() => true);
//     const error = await search('search', null, spinnerInstance);
//     expect(error.name).toBe(error);
//     expect(error.message).toBe(
//       'Incorrect callback function passed to list command.'
//     );
//   });
// });
